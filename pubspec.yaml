name: mgrs_maintenance
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^2.17.2
  mobile_scanner: ^7.4.0
  uuid: ^4.6.0
  url_launcher: ^6.3.1
  rive: 0.13.20
  lottie: ^3.5.1
  intl: ^0.19.0
  http: ^1.2.0
  web: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter

  flutter_lints: ^6.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/animation/
    - assets/animation/auth_teddy.riv
    - assets/animation/splash_mgrs.json
    - assets/icons/
